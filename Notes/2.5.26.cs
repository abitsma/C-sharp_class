using System;
public class Example
{
    //in C# we can turn ANY object into a string
    //For ANY class we make we can choose how ToString() for it works
    //Here's some code that kinda yeehaw's 
    //So ToString() is like a pretty standard thing
    public override string ToString()
    {
        return base.ToString(); //Imagine <TODO> idk
    }

}

public class Program
{
    //Instead of doing if statements yo ucan use switch and case
    int x = 5;


    //You can also do something like this
    //string filename = "myFile.txt";
    //string[] lines = System.IO.File.ReadAllLines(filename);
    //foreach (string line in lines)
    //{
    //string[] parts = line.Split
    //}

    //string filename = myFile.txt;
    //using StreamWriter outputFile = new StreamWriter(filename))
    //{
    //outputFile.WriteLine("This will be the first line in the file.");
    //string color = "Blue";
    //outputFile.WriteLine($"My favorite color is {color}.");
    //}
}