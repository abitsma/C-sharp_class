//so when you draw a class diagram for an interface class
//you like... put <<interface>>
//Then the class name
//Maybe we have a person, company, and a phone
//all have a phone number... but none of then cleanly inherit from anything
//so instead of a parent-child class, we do a class interface
//We use dotted lines to show interfaces
//HasPhoneNumber can be the interface for Person, COmpany, and Phone
//HasPhoneNumber can have the method GetPhoneNumber(): string
